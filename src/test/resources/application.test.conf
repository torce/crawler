include "reference"

akka {
  actor {
    provider = "akka.actor.LocalActorRefProvider"
  }
  loglevel = INFO
}

scrawl {
  master.couch.db-name = "test"
  robots-filter {
    retry-timeout = 500
  }
}