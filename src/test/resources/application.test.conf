include "application"

akka {
  actor {
    provider = "akka.actor.LocalActorRefProvider"
  }
  loglevel = INFO
}

prototype {
  master.couch.db-name = "test"
  robots-filter {
    retry-timeout = 500
  }
}